# install python build utility
python3 -m pip install build
python3 -m pip install --upgrade build

# build package
python3 -m build

# install upload utility twine
python3 -m pip install twine
python3 -m pip install --upgrade twine
# check distributions
On MacOS: twine check dist/*
On Windows: twine check . dist/*
# upload packages to testpypi or pypi
python3 -m twine upload --repository testpypi dist/*


### nrobo framework commands
# bundle package
python package_upload.py -b -t test

# check package
